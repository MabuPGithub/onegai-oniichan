<div class="back-container">
    <%= link_to "◄ Back", "/" %>
  </div>
<h1 id="pending-h1">In-Stock</h1>

<% @in_stock.each do |nendo| %>
  <div class="card">
    <%= image_tag nendo.main_photo %>
    <div class="card-container">
    <div class="vl"></div>
      <span>Nendoroid <%= nendo.nendoroid_number %><span><br>
      <span><%= nendo.name %><span>
      <%= button_to "Add to cart", line_items_path, params: {:id => nendo.id}, method: :post %>

    </div>
  </div>
<% end %>