<span class=menu-item><%= link_to "Logout", destroy_user_session_path(), method: :delete %></span>
<h1> Admin Page </h1>

<%= link_to "View Transactions", all_transactions_url %>

<%= link_to "Order List", orders_url %>

<%= link_to "Transaction Status", transaction_status_url %>

<%= link_to "Upload a Nendo", upload_nendo_url %>



<h1 id="admin-users-h1">Users</h1>
<table id="customers">
  <tr>
    <th>Name</th>
    <th>Email</th>
    <th>Balance</th>
    <th>Action</th>
  </tr>
  <% @users.each do |user| %>
  <tr>
    <td><strong><%= user.name %></strong></td>
    <td><strong><%= user.email %></td>
    <td><strong><%= user.balance %></td>
    <td><strong>View user</td>
  </tr>
  <% end %>
</table>